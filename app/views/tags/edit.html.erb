<h1>Edit Tag</h1>

<%= form_with model: @tag, local: true do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit 'Update Tag' %>
  </div>
<% end %>
